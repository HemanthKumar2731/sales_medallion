# This is a Databricks asset bundle definition for sales_medallion.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: sales_medallion
  uuid: bf461c6a-852d-456c-9d04-f820a2f38183

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  warehouse_id:
    description: The warehouse to use
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://adb-1963913809167525.5.azuredatabricks.net
    variables:
      warehouse_id: 103aa213d78bb659
      catalog: hive_metastore
      schema: default

  prod:
    mode: production
    workspace:
      host: https://adb-1963913809167525.5.azuredatabricks.net
      # We explicitly deploy to /Workspace/Users/hemanth.chittimella@pal.tech to make sure we only have a single copy.
      root_path: /Workspace/Users/hemanth.chittimella@pal.tech/.bundle/${bundle.name}/${bundle.target}
    variables:
      warehouse_id: 103aa213d78bb659
      catalog: hive_metastore
      schema: default
    permissions:
      - user_name: hemanth.chittimella@pal.tech
        level: CAN_MANAGE
